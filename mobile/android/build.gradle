// Top-level build file. Versions are pinned so the GitHub Actions runner and a
// local build produce identical output.
plugins {
    id 'com.android.application' version '8.5.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
}
